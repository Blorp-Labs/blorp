export const COMMENT_COLLAPSE_EVENT = "COMMENT_COLLAPSE_EVENT";

/**
 * Tailwind classes applied to interactive elements inside a post card
 * that uses the stretched-link pattern. Raises them above the title
 * link's ::after overlay (z-[1]) so they remain independently clickable.
 */
export const ABOVE_LINK_OVERLAY = "relative z-[2]";

export const MAX_REACTIONS = 10;
